<div class="packages-index-container">

  <% @offres.each do |offre| %>
   <div class="package-container">
      <div class="package-img" style="background-image: url(<%= asset_path offre.photo %>)">
        <div class="package-info">
          <div class="package-title">
            <h2><%= offre.title %></h2>
          </div>
          <p><%= offre.description %></p>
          <div class="package-button">
            <%= link_to "Réserver", package_path(offre), class: "button"%>
          </div>
        </div>
        <div class="hover-package"></div>
      </div>
    </div>
  <% end %>
</div>
