<div class="packages-index-container">

  <% @packages.each do |package| %>
   <div class="package-container">
      <div class="package-img" style="background-image: url(<%= asset_path package.photo %>)">
        <div class="package-info">
          <div class="package-title">
            <h2><%= package.title %></h2>
          </div>
          <p><%= package.description %></p>
          <div class="package-button">
            <%= link_to "Réserver", package_path(package), class: "button"%>
          </div>
        </div>
        <div class="hover-package"></div>
      </div>
    </div>
  <% end %>
</div>
