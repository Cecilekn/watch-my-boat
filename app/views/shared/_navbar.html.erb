<div class="navbar navbar-expand-sm navbar-lewagon navbar-lewagon-color">
  <%= link_to "/", class: "navbar-brand" do %>
    <%= image_tag("white-logo.png") %> <span class="navbar-brand-title">Watch My Boat</span>
    <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <i class="fas fa-bars"></i>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Nos offres", packages_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Messages", "#", class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <%= cl_image_tag current_user.photo, crop: :thumb, gravity: :face, width: 50, height: 50, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <% if current_user.manager %>
              <%= link_to "Tableau de bord", "/manager/dashboard", class: "dropdown-item" %>
            <% else %>
              <%= link_to "Tableau de bord", "/dashboard", class: "dropdown-item" %>
            <% end %>
            <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>

        </li>
      <% end %>
    </ul>
  </div>

</div>
